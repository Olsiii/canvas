# Keep a single drizzle-orm instance so TypeScript doesn't see two
# incompatible SQL<> types (Sentry's @opentelemetry/api peer otherwise
# forks a second drizzle-orm copy under .pnpm).
public-hoist-pattern[]=*drizzle-orm*
